# Build artifacts
/bin/
/tmp/

# Environment secrets — never commit real values, only .env.example
.env
.env.*
!.env.example

# Go
*.test
*.out
vendor/

# Local backups (scripts/backup.sh) — real data, even when encrypted
/backups/
